{
  "name": "my-express-mongodb-app",
  "version": "1.0.0",
  "description": "An Express server with MongoDB integration",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "npm install && node server.js",
    "dev": "npm install && nodemon server.js",
    "build": "npm install && npm install cross-env && npm install --prefix ../frontend && npm run build --prefix ../frontend"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.9.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
